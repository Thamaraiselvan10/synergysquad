---
// src/components/DepartmentBanner.astro
---

<div class="banner-container">
    <h2 class="banner-text">Department of Computer Science and Engineering</h2>
</div>

<style>
    .banner-container {
        background-color: #111827;
        padding: 0.75rem 1.5rem;
        text-align: center;
        border-bottom: 1px solid #374151;
    }

    .banner-text {
        margin: 0;
        font-size: 1rem;
        font-weight: 500;
        letter-spacing: 0.05em;
        color: #d1d5db;

        /* --- START: UPDATED SHIMMER ANIMATION --- */
        background-image: linear-gradient(
            -75deg,
            #d1d5db 35%,  /* Start the highlight a bit later */
            #fff 50%,     /* The brightest point is in the middle */
            #d1d5db 65%   /* End the highlight a bit sooner */
        );
        
        /* Make the background gradient much larger than the text */
        background-size: 300% 100%; 
        
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-fill-color: transparent;
        
        /* Slow down the animation for a more elegant effect */
        animation: shimmer 7s linear infinite; 
    }

    @keyframes shimmer {
        from {
            /* Start with the gradient all the way to the right */
            background-position: 100% 0;
        }
        to {
            /* End with the gradient all the way to the left */
            background-position: 0% 0;
        }
    }
    /* --- END: UPDATED SHIMMER ANIMATION --- */
</style>